# V6 T2V 评测平台 — 产品需求文档 (PRD)

## 一、产品定位

长期承载多轮、多模型、多项目的 T2V 视频评测平台。支持 20+ 标注员并发使用，管理员可管理多个评测批次，追踪历史版本对比。

---

## 二、核心数据结构（重构）

### 概念关系

```
题库 (QuestionBank)
├── 可以被多次评测复用
├── 可以被更新（增删检查点、修正成功线）
├── 包含: 题目 + 检查点 + 能力映射
└── 有版本号（每次更新 +1，历史可追溯）

评测批次 (EvalBatch)
├── = 一次完整的评测任务
├── 绑定: 一个题库 + 一个模型版本 + 一组视频URL
├── 包含: 分配 + 标注 + 定案 + 得分
└── 状态: 准备中 → 标注中 → 已完成

版本对比
└── 选两个批次 → 并排看 30 项能力 Δ 变化
```

### 数据表设计

```
题库表: bank_id, name, version, description, updated_at
  ↓
题目表: question_id, bank_id, prompt, language, preprocess_note
  ↓
检查点表: checkpoint_id, question_id, text, min_success_line, ability_id, tag_id...

评测批次表: batch_id, name, bank_id, model_version, video_source, status, created_at
  ↓
视频表: video_id, batch_id, question_id, oss_url
  ↓
分配表: assignment_id, video_id, annotator_id, role, status
  ↓
标注表: annotation_id, assignment_id, checkpoint_id, score, note
  ↓
定案表: final_id, video_id, checkpoint_id, final_score, method
```

### 与当前结构的区别

| 当前 | 重构后 |
|------|--------|
| Project 混合了题库和评测 | 题库和评测批次分开 |
| 同一题库不同模型要建两个项目 | 同一题库可跑多个批次 |
| 无法对比 | 两个批次直接对比 |
| 题库更新 = 删项目重来 | 题库有版本，更新不影响已完成的批次 |
| 视频URL和题目绑死 | 视频属于批次（不同模型的视频不同） |

---

## 三、管理员界面结构

### 左边栏（重组后）

```
📚 题库管理
   ├── 题库列表（查看/新建/更新）
   └── 导入检查点 Excel

📋 评测批次
   ├── 批次列表（状态/进度一览）
   ├── 新建批次（选题库 + 模型版本 + 上传视频URL）
   └── 当前批次详情
        ├── 分配任务
        ├── 进度监控
        ├── 标注详情
        └── 导出结果

📊 版本对比
   └── 选两个批次 → 能力得分 Δ 对比

👥 标注员管理
   ├── 人员列表（增删改查）
   └── 工作量统计
```

### 管理员首页 = 批次列表

| 批次名 | 题库 | 模型版本 | 状态 | 进度 | 创建时间 |
|--------|------|---------|------|------|---------|
| B001 | T2V-10s v2 | model_v12 | 已完成 | 100% | 2026-08-10 |
| B002 | T2V-10s v2 | model_v13 | 标注中 | 45% | 2026-08-11 |
| B003 | T2V-10s v3 | model_v14 | 准备中 | 0% | 2026-08-11 |

点进某个批次 → 看到该批次的任务分配、监控、导出。

---

## 四、标注员界面

### 左上角显示当前批次

标注员登录后看到分配给自己的任务，按批次分组：

```
📋 B002 - model_v13 评测 (15/22 已完成)
   └── 任务卡片列表...

📋 B001 - model_v12 评测 (22/22 已完成 ✓)
```

### 标注员不看模型得分

结果看板改为**个人统计**：
- 我完成了多少 / 总共多少
- 我的 C/R/N/NA 分布
- 我的一致率（双人模式下）

---

## 五、题库更新策略

题库更新时：
- 已完成的批次**不受影响**（冻结在创建时的题库版本）
- 进行中的批次**可选择**是否同步更新（一般不建议）
- 新批次**自动用最新版本**

更新内容可以是：
- 增加检查点
- 修改最低成功线
- 删除不合理的检查点
- 更正能力/标签映射

---

## 六、版本对比看板

选择两个批次后展示：

| 能力 | 批次A (v12) | 批次B (v13) | Δ | 变化 |
|------|------------|------------|-----|------|
| C01 | 65.0 | 72.3 | +7.3 | ↑ |
| C06 | 58.2 | 55.1 | -3.1 | ↓ |
| ... | ... | ... | ... | ... |

---

## 七、当前代码的具体问题

| 问题 | 影响 | 优先级 |
|------|------|--------|
| 翻页跳转不稳定 | 标注员卡住 | P0 |
| 不支持追加人员到已分配批次 | 管理员灵活性差 | P0 |
| 无题库/批次分离 | 多模型对比无法实现 | P1 |
| 导入后无法预览/确认 | 导错不可逆 | P1 |
| 左边栏 8 项平铺无层级 | 找不到功能 | P1 |
| 视频播放器无倍速 | 标注效率低 | P2 |
| 无键盘快捷键 | 标注效率低 | P2 |
| 无操作日志 | 出事无法追溯 | P3 |

---

## 八、实施计划

### Phase 1（本次）：数据结构重构

- 拆分 Project → QuestionBank + EvalBatch
- 左边栏重组
- 批次列表/详情页
- 保持标注流程不变

### Phase 2：版本对比

- 对比看板
- 条件导出

### Phase 3：标注体验

- 键盘快捷键
- 视频增强
- 个人统计替换结果看板

### Phase 4：平台级

- 操作日志
- 通知
- 金标题
